-- Initial database schema for Phase 0 (Placeholder)
-- Actual tables will be created in Phase 1

CREATE TABLE dummy_init (
    id SERIAL PRIMARY KEY,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
